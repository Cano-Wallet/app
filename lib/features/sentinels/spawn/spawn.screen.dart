import 'package:flutter/material.dart';

class SpawnSentinelScreen extends StatelessWidget {
  const SpawnSentinelScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Spawn'),
      ),
      body: const Center(
        child: Text('Spawn'),
      ),
    );
  }
}
