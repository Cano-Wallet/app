import 'package:flutter/material.dart';

class SpawnPillarScreen extends StatelessWidget {
  const SpawnPillarScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Spawn'),
      ),
      body: const Center(
        child: Text('Spawn'),
      ),
    );
  }
}
